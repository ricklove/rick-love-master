
export enum IconKind {
    // common icons
    menu,
    back,
    error,
    expanded,
    collapsed,
    copy,
    retry,
    dashboard,
    add,
    remove,

    // navigation icons
    account,
    message,
    subscription,
    calendar,
    contacts,
    keys,
    resources,
    admin,
}
