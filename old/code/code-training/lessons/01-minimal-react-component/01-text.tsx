import React from 'react';

export const MinimalReactComponent = (props: {}) => {
    return (
        /* # Text
        FOCUS: Hello World!
        Objective: Enter the Text
        Explanation:
        We want to display 'Hello World!' as text using the simplest React possible.
        Let's start with the text.
        Type Hello World!
        Experiments:
        World => Universe
        Hello => Sup
        */
        <span>Hello World!</span>
        // # END
    );
};
