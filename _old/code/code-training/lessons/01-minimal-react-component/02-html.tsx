import React from 'react';

export const MinimalReactComponent = (props: {}) => {
    return (
        /* # Html
        Objective: Enter the Html
        Explanation:
        We want to display 'Hello World!' as text using html.
        Enter the html to display 'Hello World!' inside a span element.
        Descriptions:
        The span element is a used to render text like 'Hello World!'.
        This span element starts with the <span> open tag and ends with the </span> close tag.
        Experiments:
        <span> => <div> &&& </span> => </div> ::: Use a div element instead of a span element.
        */
        <span>Hello World!</span>
        // # END
    );
};
