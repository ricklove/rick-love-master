{
  "name": "@ricklove/game-tools-cli",
  "version": "1.0.0",
  "type": "module",
  "main": "./lib/index.js",
  "scripts": {
    "build": "tsup index.ts worker.ts --sourcemap --format esm --outDir=lib",
    "build:watch": "tsup index.ts worker.ts --sourcemap --format esm --outDir=lib --watch",
    "cli": "npm run build && node ./lib/index.js"
  },
  "dependencies": {
    "simfile-parser": "0.4.1"
  },
  "devDependencies": {
    "@ricklove/config-lib-rig": "workspace:*",
    "@types/node": "12.12.6",
    "tsup": "6.7.0",
    "ts-node": "~10.9.1",
    "typescript": "~4.9.5"
  }
}