{
  "name": "@ricklove/art-contract",
  "version": "1.0.0",
  "main": "./lib/index.js",
  "scripts": {
    "compile": "truffle compile",
    "size": "npm run compile && env-cmd -f .env truffle run contract-size",
    "deploy:dryrun": "env-cmd -f .env truffle deploy --network rinkeby --dry-run",
    "deploy:testnet": "env-cmd -f .env truffle deploy --network rinkeby"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.3.2",
    "@truffle/hdwallet-provider": "^1.2.1",
    "solc": "^0.8.7-fixed",
    "truffle": "~5.4.10"
  },
  "devDependencies": {
    "@types/node": "12.12.6",
    "dotenv": "^8.2.0",
    "env-cmd": "^10.1.0",
    "truffle-contract-size": "^2.0.1"
  }
}